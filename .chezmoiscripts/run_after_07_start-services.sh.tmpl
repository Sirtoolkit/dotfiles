#!/bin/bash
# Activa los servicios necesarios si están instalados
set -e

if command -v aerospace >/dev/null 2>&1; then
  echo "Starting AeroSpace..."
  open -a AeroSpace &
  sleep 2
else
  echo "AeroSpace not found, skipping."
fi
