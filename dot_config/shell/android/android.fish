if test -f "$HOME/.config/shell/android/android_new_avd.fish"
    source "$HOME/.config/shell/android/android_new_avd.fish"
end

if test -f "$HOME/.config/shell/android/android_launch_emulator.fish"
    source "$HOME/.config/shell/android/android_launch_emulator.fish"
end

if test -f "$HOME/.config/shell/android/android_delete_emulator.fish"
    source "$HOME/.config/shell/android/android_delete_emulator.fish"
end

if test -f "$HOME/.config/shell/android/android_delete_system_image.fish"
    source "$HOME/.config/shell/android/android_delete_system_image.fish"
end

if test -f "$HOME/.config/shell/android/android_enable_keyboard.fish"
    source "$HOME/.config/shell/android/android_enable_keyboard.fish"
end
